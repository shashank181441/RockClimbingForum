# HTTP → HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name rock.vendingao.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS (certs already issued by certbot)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name rock.vendingao.com;

    ssl_certificate /etc/letsencrypt/live/rock.vendingao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rock.vendingao.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
